#include <stdio.h>

int main(){
    char m[20] = {'0','1','2','3','4','5','6','7','8'};

    // int a = m[0] - '0';
    int a = (int) m[0];

    // printf("%c", m[0]);
    
    printf("%c", a);

    // printf("%d", a);
}